.venv/
__pycache__/
scouting-output*/
heritage-results*/
*.xlsx
scouts*.csv
pdf-uploads/
runs/
/build/
/dist/
# Added by code-review-graph
.code-review-graph/
# No trailing slash: also covers a symlink to the corpus, which worktrees need
# (a trailing slash matches directories only, leaving a symlink committable).
/raw_data
.DS_Store
